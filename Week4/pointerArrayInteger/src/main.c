#include <stdio.h>
#include "source.h"


/* Objective: pointer arrays
 * Initiate pointer array to integers and pass it to function smallest_number 
 * that will then locate the smallest number and return the value as parameter 
 * Function should return the smallest number in the array.
 * NOTE the correct signature is only in the header file*/


int main(void) {
//    int size = 8;
//    int *array[size];

/*array [0] = 2;
array [1] = -8;
array [2] = 5;
array [3] = 3;
array [4] = -1;
array [5] = 9;
array [6] = 2;
array [7] = 7;


    int smallest = smallest_number(array, size);
    printf("%d\n", smallest);*/
    return 0;
}
